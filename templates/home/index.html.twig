{# templates/home/index.html.twig #}

<!DOCTYPE html>
<html>
<head>
    <title>{{ pageTitle }}</title>
</head>
<dody>
    <h1>{{ pageTitle }}</h1>

    <a href="{{ path('app_form') }}">Lien vers le formulaire</a></br>

    <a href="{{ path('product') }}">Lien vers les produits</a></br>

    <a href="{{ path('command') }}">Faire une commande</a></br>



    <p>{{ message }}</p>
</dody>
</html>